<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="./public/stylesheets/gsaSamplingContractTemplate.css">
    <title>Document</title>
</head>
<body>
    <div class="page">
        <div class="title">
            <img src="./public/assets/logo.png" alt="gsa-logo">
            <h3>Contract form for Measurement Services</h3>
            <div class="bottom">
                <p><strong>Document Number:</strong> {{documentNumber}}</p>
                <p><strong>Date:</strong> {{date}}</p>
            </div>
        </div>

        <div class="header">
            <div class="customer">
                <h3>Customer Details:</h3>
                {{#if customer.company}}
                    <p><strong>Company:</strong> {{customer.company}}</p>
                {{/if}}
                {{#unless customer.company}}
                    <p><strong>Name:</strong> {{customer.name}} {{customer.surname}}</p>
                {{/unless}}
                <p><strong>TIN:</strong> {{customer.tin}}</p>
                <p><strong>Address:</strong> {{customer.address}}</p>
                <p><strong>Phone Number:</strong> {{customer.phone}}</p>
            </div>
            <div class="weight">
                <p style="margin-top: 20px;"><strong>Sample weight (in grams):</strong> {{registration.Sample_weight}}</p>
            </div>
        </div>

        <div class="table-container">
            <table>
                <tbody>
                    <tr>
                        <th class="table-row-label">Sample Name</th>
                        <th class="table-data">{{registration.Customer_sample_name}}</th>
                    </tr>
                    <tr>
                        <th class="table-row-label">GSA Sample ID</th>
                        <th class="table-data">{{registration.gsa_sample_id}}</th>
                    </tr>
                    <!-- <tr *ngFor="let m of method; let isFirst = first"> -->
                    <tr >
                        <!-- <ng-container *ngIf="isFirst"> -->
                            <th class="table-row-label">Measurement Service</th>
                            <th class="table-data">
                                <p class="measurementData">{{measurementService}}</p>
                            </th>
                        <!-- </ng-container> -->
                    </tr>
                    <tr>
                        <th class="table-row-label">Assigned Method</th>
                        <th class="table-data">
                            <p class="measurementData">{{selectedMethodTypes}}</p>
                        </th>
                    </tr>
                    <tr>
                        <th class="table-row-label">Approved Quotation Value</th>
                        <th class="table-data">{{registration.Approved_Quotation_Value}} {{currency}}</th>
                    </tr>
                    <tr>
                        <th class="table-row-label">Approximate Dates of Work Completion</th>
                        <th class="table-data">{{registration.Approx_days}}</th>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="agreement-container">
            <p><strong>Agreed between:</strong></p>
            <div class="agreement-container-content">
                <div class="left">
                    <h4>Company or customer representative:</h4>
                    <h4>[Name and Signature]</h4>
                </div>
                <div class="right">
                    <h4>GSA representative:</h4>
                    <h4>[Name and Signature]</h4>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>Document Number: {{documentNumber}} Rev: 00, Page: 1-1</p>
        </div>
    </div>
</body>
</html>